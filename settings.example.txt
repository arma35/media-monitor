# Local settings (copy from settings.example.txt). Never overwrite on update.
#
# article_date_not_later_than — YYYY-MM-DD  ("дата ДО")
#   Keep hits with publish date on or before this day.
#   Empty value = today.
#
# article_date_not_older_than — YYYY-MM-DD  ("не старше")
#   Keep hits with publish date on or after this day.
#   Empty value = no lower bound.
#
# Example: from 2020-01-01 through today
#   article_date_not_later_than=
#   article_date_not_older_than=2020-01-01
#
# If publish date is not found:
#   - when article_date_not_older_than is set → hit is EXCLUDED
#   - otherwise → hit is kept
#
# max_scan_urls — max pages to scan in one run. 0 = no limit.
# max_expand_links — max links taken from one listing/search page.
#
# auth_timeout_seconds — how long to wait for login/password at start (default 180).

article_date_not_later_than=
article_date_not_older_than=
max_scan_urls=0
max_expand_links=120
auth_timeout_seconds=180
