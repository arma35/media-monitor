# Local settings (copy from settings.example.txt → setting.txt / settings.txt).
# Never overwritten on update if the local file already exists.
#
# article_date_not_later_than — YYYY-MM-DD  ("дата ДО")
#   Empty = today.
#
# article_date_not_older_than — YYYY-MM-DD  ("не старше")
#   Empty = no lower bound.
#
# If publish date is not found — hit is still kept in the report.
#
# max_scan_urls — 0 = no limit
# max_expand_links — max links from one listing/search page
# auth_timeout_seconds — login/password wait at start

#дата ДО, если не задано то сегодня
article_date_not_later_than=

#дата НЕ старше
article_date_not_older_than=

max_scan_urls=0
max_expand_links=120
auth_timeout_seconds=180
