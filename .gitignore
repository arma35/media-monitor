__pycache__/
*.py[cod]
.venv/
venv/
dist/
build/
*.spec

# Local configs — never overwrite / never commit user lists
sites.txt
words.txt
settings.txt
exclude.txt
setting.txt

# Runtime log (appendable, auto-trimmed at 10MB)
media-monitor_log.txt
media-monitor.txt

# Local SQLite scan cache (URLs already checked)
scan_cache.sqlite
scan_cache.sqlite-*

# Generated reports
reports/*
!reports/.gitkeep
